exports.up = function(knex) {
  // No-op placeholder to match migration recorded in database migration history.
  return Promise.resolve();
};

exports.down = function(knex) {
  return Promise.resolve();
};
