exports.up = function(knex) {
  // Placeholder to repair missing migration file referenced in DB migration history.
  return Promise.resolve();
};

exports.down = function(knex) {
  return Promise.resolve();
};
